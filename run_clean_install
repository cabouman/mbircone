#!/bin/bash

conda env create -n test_mbircone python=3.8 pip &&

source activate test_mbircone &&

pip install -r requirements.txt &&

# pip install . &&

python setup.py install &&

cd demo &&

python demo.py

cd .. 
